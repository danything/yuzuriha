# compose.override.yml として写して使う。値は Git に入らない。
#
#   cp compose.override.yml.example compose.override.yml
#
services:
  yuzuriha:
    environment:
      # 毎朝9時に取得したい場合
      BUILD_CRON: "0 9 * * *"
      # 取得の間隔を広げたい場合
      # FETCH_DELAY_MS: "1000"
